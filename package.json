{
  "name": "novel-library",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build:packages": "pnpm --filter @novel/db build && pnpm --filter @novel/sources build && pnpm --filter @novel/translation build && pnpm --filter @novel/core build",
    "build": "pnpm build:packages && pnpm --filter @novel/web build && pnpm --filter @novel/worker build",
    "dev": "pnpm build:packages && node --env-file-if-exists=.env node_modules/concurrently/dist/bin/concurrently.js -k -n db,sources,translation,core,web,worker \"pnpm --filter @novel/db dev\" \"pnpm --filter @novel/sources dev\" \"pnpm --filter @novel/translation dev\" \"pnpm --filter @novel/core dev\" \"pnpm --filter @novel/web dev\" \"pnpm --filter @novel/worker dev\"",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm build:packages && vitest run",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:migrate": "pnpm build:packages && node --env-file-if-exists=.env packages/db/dist/migrate.js"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "concurrently": "9.2.1",
    "eslint": "9.39.5",
    "eslint-config-next": "16.3.5",
    "eslint-config-prettier": "10.1.8",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.70.0",
    "vitest": "4.0.18"
  }
}
